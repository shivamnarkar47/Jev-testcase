# Goal: Fix typo in README header

## Steps
1. In `README.md`, change `# Jev Plan Validtor` to `# Jev Plan Validator`.
2. Run `git diff` to confirm only that line changed.

## Verification
- `git diff --stat` shows 1 file, 1 line.
